// -- ./src/template/landing-page/color-schemas/theme-change.tsx
import { cn } from 'src/lib/utils'
import React from 'react'
import { themes } from 'src/config/thems'
import { Check } from 'lucide-react'

import {
  TooltipProvider,
  Tooltip,
  TooltipContent,
  TooltipTrigger,
} from 'src/template/ui/tooltip'

const ThemeChange = () => {
  //  "--theme-primary": `hsl(${newTheme?.cssVars[mode === "dark" ? "dark" : "light"].primary

  return <div>No theme</div>
}

export default ThemeChange
